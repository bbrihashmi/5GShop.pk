name = "5gshop-pk"
main = "src/worker.js"
compatibility_date = "2024-11-01"

[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true

# Bind D1 in Cloudflare Dashboard:
# Settings → Bindings → D1 Database
# Variable name must be exactly: DB
